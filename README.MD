# Simply FastAI / FastGradio demo

Requires `fastai` and `fastgradio` be installed. 

Codespaces: Regrettably, it looks like Codespaces doesn't provide enough memory to run FastAI, so that looks like a dead end. 
